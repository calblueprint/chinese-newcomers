import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    borderColor: '#A1A1A1',
    height: '5%',
    width: '80%',
    borderWidth: 0.5,
    marginTop: '4%',
    marginBottom: '4%',
    alignItems: 'center'
  }
});

export default styles;
