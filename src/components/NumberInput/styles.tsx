import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
    // height: '100%',
    marginTop: 3
  },
  input: {
    borderWidth: 2,
    borderRadius: 3,
    borderColor: '#49260C',
    width: '100%',
    height: 40
  }
});
